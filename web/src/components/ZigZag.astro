---
// Pastel tribal zigzag band — echoes the logo's triangle border.
// Tooth colors rotate peach → mint → gold → peach-soft → sky → apricot,
// the six-tint sequence locked in the design system (/design-system/).
const { flip = false } = Astro.props;
---
<svg class:list={["zigzag", { flip }]} width="100%" height="18" aria-hidden="true" role="presentation">
	<defs>
		<pattern id="zz" patternUnits="userSpaceOnUse" width="120" height="18">
			<path d="M0,18 L10,0 L20,18 Z" fill="var(--peach)"></path>
			<path d="M20,18 L30,0 L40,18 Z" fill="var(--mint)"></path>
			<path d="M40,18 L50,0 L60,18 Z" fill="var(--gold)"></path>
			<path d="M60,18 L70,0 L80,18 Z" fill="var(--peach-soft)"></path>
			<path d="M80,18 L90,0 L100,18 Z" fill="var(--sky)"></path>
			<path d="M100,18 L110,0 L120,18 Z" fill="var(--apricot)"></path>
		</pattern>
	</defs>
	<rect width="100%" height="18" fill="url(#zz)"></rect>
</svg>
<style>
	.zigzag {
		display: block;
		width: 100%;
		height: 18px;
	}
	.zigzag.flip {
		transform: scaleY(-1);
	}
</style>
